package wasteland.global;

public class Constants {
    // FPS is the frames per second.
    public static final int fps = 10;
    // ViewWidth is the width of the view in tiles.
    public static final int viewWidth = 40;
    // ViewHeight is the height of the view in tiles.
    public static final int viewHeight = 30;
    // RendererMode is the mode in which the renderer 
    // is going to print to terminal.
    // 0 = Overwrite terminal output
    // 1 = Clear and print to terminal.
    // 2 = 0 and 1 combined (default)
    public static final int rendererMode = 2;
}
